Task and capture responses parsed Duration as an integer, so the valid `longer` bucket caused task reads to fail with 500 and capture to report 503. Both responses now return the bucket string or null, preserving the distinction between `longer` and an unprocessed Task. Generated API types match the server; incoming request shapes are unchanged.

Closes #130.

Validation: `dotnet test task-guide.slnx` passed all 595 tests across five projects; frontend 125 tests, build and lint passed. Regression coverage includes 61-minute capture, `longer` task reads, null capture/read round trips, and nullability in both response schemas. Standards and Spec reviews completed with no unresolved findings. Original red evidence and an old-capture-behavior mutation confirmed regression detection.

Integration-owned schema regeneration was performed as a serialized phase on this branch. No client.ts, Program.cs, request-shape or #101 worktree changes. Phil authorized committing, merging and pushing issue-compliant work; deployment remains separate.
