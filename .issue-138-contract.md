## Implementation contract and ownership — coordinator + Opus, 2026-09-14

#130 is merged. This ticket now has zero open blockers. Opus reviewed the following bounded contract; it follows the issue's explicit option of a dedicated route and the consumer's five-bucket requirement.

- `PUT /api/tasks/{id}/duration`, body `{ "duration": "2" | "10" | "30" | "60" | "longer" }`.
- Accept exact tokens from `KnownDimensions.DurationBuckets`. Sized bucket tokens pass through existing `DurationCeiling.SnapUp`; the declared unsized `longer` bucket is preserved directly, without a numeric sentinel. Raw-minute input and clearing Duration are outside this ticket. Unknown/case/whitespace tokens, omitted/null values, numeric bodies, malformed JSON and invalid Task IDs return 400. Framework model-binding failures may retain the framework's existing 400 body; no global/custom JSON binder is needed.
- Success: 204. A well-formed but absent stored Task or a derived Task that cannot be edited returns typed 409 with a reason, following the existing Defer/Postpone write convention. No new completed/recurring Status restrictions are introduced.
- The command finds and updates inside one `IStore.MutateAsync` closure. Replace only the Duration dimension with exactly one canonical value. Preserve other dimensions, loose tags, completion facts, every other Task field and every other Task. Never write Status; subsequent reads derive it. Repeating the same bucket succeeds.
- No new port, DI registration, package, project, or schema migration. Existing command seam and endpoint group are reused. UI wiring stays with #101/#102.

**Implementation owns:** `src/TaskGuide.Application/Tasks/SetTaskDuration.cs` (new), `src/TaskGuide.Api/Endpoints/TaskEndpoints.cs` (after #130), focused new command/API test files, relevant `tests/TEST-INVENTORY.md` entries. Tests exercise the public command via an in-memory store and the HTTP route through the existing test host: all five buckets, longer preservation, null-before/after repair, invalid input/refusals without mutation, and preservation of unrelated data. Add an OpenAPI assertion for the route's declared outcomes.

**Integration owns, serialized after implementation is frozen:** `src/TaskGuide.Web/src/api/schema.d.ts` regeneration on the same branch, followed by frontend tests/build/lint and final independent review. `client.ts`, `Program.cs`, `.csproj` and #101's existing worktree remain untouched.

TDD one behavior at a time; record actual red/green output immediately. Validate with explicit `dotnet test task-guide.slnx` so unrestored test projects are not silently omitted. Closing evidence must name this route/body and schema so #101 can wire its five buttons. Commits, PR merge and push are authorized for issue-compliant validated work; deployment/deletion/spending remain separate.
